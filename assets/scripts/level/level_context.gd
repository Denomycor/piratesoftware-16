## class_name intentionally omitted: Godot 4.6.3 reports "hides an autoload
## singleton" when class_name matches the autoload name (case-insensitive match
## between levelContext and LevelContext). Access via autoload name LevelContext.
extends Node

var level: Level
